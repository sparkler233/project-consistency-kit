# 一致性机制 version: 2026-08-19
interface:
  display_name: "Wrap Up"
  short_description: "Review linkage and close the current work cycle"
  default_prompt: "Use $wrapup to reconcile linked files and prepare a confirmed local checkpoint."
