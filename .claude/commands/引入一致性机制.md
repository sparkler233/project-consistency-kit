---
description: 引入一致性机制——调用可分发的安装器 Skill
---

<!-- 一致性机制 version: 2026-08-18 -->

解析全局入口 `~/.claude/commands/引入一致性机制.md` 的最终符号链接目标,由该文件所在的 `.claude/commands/` 向上两级取得套件根。完整读取并严格执行 `<套件根>/skills/project-consistency-installer/SKILL.md`。该 Skill 是安装行为唯一正本;本文件只保留 Claude Code 兼容入口,不得复制或改写安装流程。若入口不是符号链接或无法定位正本,停止并提示用户改用全局 `project-consistency-installer` Skill。
