# ─────────────────────────────────────────────────────────────
# .gitignore · 选择性二进制策略(来自「项目一致性机制」)
#   · 栅格图(png/jpg/…)→ 走 Git LFS,进 git(见 .gitattributes),故「不」在此忽略。
#   · 设计源 / PDF / Office / 字体 / 媒体 → 体积大或可替换,排除 + 用 _manifest.md 翻译。
#   · _drafts/ 整体忽略(试稿不进 git,也不占 LFS 额度)。
# 需要破例纳入某个被忽略的二进制,用  git add -f  显式添加。
#
# 〔按项目裁剪〕纯代码 / 纯文字项目通常没有要管理的图片 / 设计源——
#   可删掉下面「设计稿 / 媒体」等行,只留系统文件区即可。
# ─────────────────────────────────────────────────────────────

# 栅格图 不在此忽略 —— 由 Git LFS 管理(见 .gitattributes)。
# _drafts/ 里的栅格图仍被下方「草稿双轨」整体挡掉,不会进 LFS。

# 设计稿(体积大、单文件可达数百 MB;留本地 + manifest,暂不进 LFS 省额度)
*.psd
*.ai
*.indd
*.idml
*.sketch
*.fig

# PDF(外来 / 印前文件;用 .md 摘要替代进 git)
*.pdf

# Office 二进制(写作主格式建议用 .md;DOCX/XLSX/PPTX 是导出物或外来附件)
*.docx
*.xlsx
*.pptx

# 字体
*.ttf
*.otf
*.woff
*.woff2

# 媒体
*.mp4
*.mov
*.mp3
*.wav
*.m4a

# 注意:.svg 是 XML 文本,git 能 diff,故「不」忽略。

# ─────────────────────────────────────────────────────────────
# 草稿双轨:试稿目录整体忽略(仅二进制文件夹使用)
# ─────────────────────────────────────────────────────────────
_drafts/

# ─────────────────────────────────────────────────────────────
# 系统 / 工具文件
# ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.tmp
*~
