# 一致性机制 version: 2026-08-22
interface:
  display_name: "Project Consistency Installer"
  short_description: "Fetch and install the consistency kit safely"
  default_prompt: "Use $project-consistency-installer to fetch the kit and integrate it into this repository."
