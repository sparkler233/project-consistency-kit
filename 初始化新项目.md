# 在新项目里启用 Project Consistency Kit

> 新会话用 catchup 读取项目事实与 Git 状态,收尾用 wrapup 检查联动与提交;只有 canonical branch 通过 guard 后推进项目级 `synced`。Codex 使用 `$catchup` / `$wrapup`,Claude Code 使用 `/catchup` / `/wrapup`。
> 机制设计见 [`一致性机制/机制设计说明.md`](一致性机制/机制设计说明.md)。

## 文件职责

```text
README.md                  项目自己的对外说明(可选,套件不创建)
PROJECT.md                 项目事实、地图、阶段与近期决策
AGENTS.md                  Agent 工作规则的唯一实体正本
CLAUDE.md                 只含 @AGENTS.md 的 Claude Code 导入适配
```

源码仓库与干净 Release 分工如下:源码 `main` 保留套件自身运行状态;`v*` Release 只包含项目无关机制件和模板。下面先展示源码树,标记为“不拷”的自举文件在干净 Release 中本来就不存在。

```text
project-consistency-kit/
├── README.md                         套件 GitHub 主页(不拷)
├── PROJECT.md                        套件自身事实(不拷)
├── AGENTS.md                         套件自身规则(不拷)
├── CLAUDE.md                          套件自身导入适配入口(不拷)
├── .agents/
│   ├── skills/
│   │   ├── catchup/                   入向工作流正本(拷)
│   │   └── wrapup/                    出向工作流正本 + synced guard(拷)
│   └── hooks/
│       ├── wrapup-reminder.mjs        跨平台 Stop 判断正本(拷)
│       └── wrapup-reminder.ps1        Windows Codex 薄适配器(拷)
├── .codex/
│   └── hooks.json                     Codex 本地 Stop hook 接线(拷)
├── skills/
│   └── project-consistency-installer/ 机器级安装器正本(不拷)
├── templates/
│   ├── PROJECT.md                    新项目事实骨架
│   ├── AGENTS.md                     新项目规则骨架
│   └── 一致性机制/
│       ├── 文件联动目录.md             新项目联动规则骨架
│       └── 决策档案.md                 新项目空白决策档案
├── .claude/
│   ├── settings.json                 Stop hook 接线
│   └── commands/
│       ├── catchup.md                 Claude Code 薄适配器(拷)
│       ├── wrapup.md                  Claude Code 薄适配器(拷)
│       └── 引入一致性机制.md           机器级安装器适配器(不拷)
├── 一致性机制/
│   ├── VERSION                         正式套件版本(拷)
│   ├── README.md
│   ├── 机制设计说明.md
│   ├── 文件联动目录.md                 套件自身真实规则(不拷)
│   ├── 决策档案.md                     套件自身历史(不拷)
│   └── hooks/收尾提醒.sh               旧 Unix 接线兼容包装
├── CHANGELOG.md                       套件版本史(不拷)
├── LICENSE                            套件许可证(复制到机制子目录)
└── 初始化新项目.md                    本指南(不拷)
```

> 已有文件 / Git 历史的项目不要走下面的 rsync。安装机器级引导器后,进入目标项目并告诉 Agent“给这个项目引入一致性机制”。支持显式 Skill 调用的 Harness 也可选择 `project-consistency-installer`;具体入口由 Harness 决定。

## 安装机器级引导器(每台机器一次)

默认通过 skills.sh 从 GitHub 安装,不需要先 clone 整套仓库。机器需有 Git、Node.js、`curl`、`tar`,以及 `sha256sum` 或 `shasum`:

```bash
npx skills add sparkler233/project-consistency-kit \
  --skill project-consistency-installer \
  --global
```

安装器在目标项目运行时会优先使用用户提供的本地套件;没有时把最新干净 GitHub Release 获取到机器缓存,验证压缩包、内部逐文件 SHA-256、正式版本、修订日期、来源元数据与 source commit 后继续增量安装。可用 `--release <v-tag>` 固定版本;校验失败不会回退到源码 `main`。安装计划会显示引导器版本、来源 Kit 版本和目标项目版本。

维护套件或需要离线开发时,macOS / Linux 可改用本地符号链接:

```bash
KIT_DIR="/你的绝对路径/project-consistency-kit"
mkdir -p ~/.agents/skills
ln -sfn "$KIT_DIR/skills/project-consistency-installer" \
  ~/.agents/skills/project-consistency-installer

mkdir -p ~/.claude/commands
ln -sfn "$KIT_DIR/.claude/commands/引入一致性机制.md" \
  ~/.claude/commands/引入一致性机制.md
```

本地开发链接都回到 `skills/project-consistency-installer/` 正本;套件迁移后需重建。skills.sh 安装不依赖本地 clone 路径。

原生 Windows 日常使用仍推荐 skills.sh。需要测试本地源码时,在 PowerShell 临时设置:

```powershell
$env:PROJECT_CONSISTENCY_KIT_DIR = 'D:\你的绝对路径\project-consistency-kit'
```

然后在目标项目调用安装器 Skill;它会把该 checkout 视为只读开发源。这样不依赖 Windows symlink 或固定的全局复制。关闭当前 PowerShell 后临时变量自动失效。

## 绿地开张

### 1. 复制项目无关机制件

原生 Windows 优先使用机器级安装器:在空项目目录中告诉 Agent“给这个项目引入一致性机制”,确认安装计划与 `git init` 即可。安装器会走 PowerShell 获取入口、创建 `@AGENTS.md` 适配器并保留所有确认边界。

macOS / Linux 需要手工绿地复制时,在全新项目根执行；`KIT_DIR` 优先指向解压后的干净 Release 目录,维护者本地开发时也可显式指向源码 checkout:

```bash
rsync -a \
  --exclude='.git' \
  --exclude='.DS_Store' \
  --exclude='README.md' \
  --exclude='PROJECT.md' \
  --exclude='AGENTS.md' \
  --exclude='CLAUDE.md' \
  --exclude='LICENSE' \
  --exclude='初始化新项目.md' \
  --exclude='CHANGELOG.md' \
  --exclude='DISTRIBUTION-METADATA.txt' \
  --exclude='DISTRIBUTION-MANIFEST.sha256' \
  --exclude='docs/' \
  --exclude='templates/' \
  --exclude='/skills/' \
  --exclude='.claude/commands/引入一致性机制.md' \
  --exclude='一致性机制/文件联动目录.md' \
  --exclude='一致性机制/决策档案.md' \
  "$KIT_DIR/" ./

cp "$KIT_DIR/templates/PROJECT.md" ./PROJECT.md
cp "$KIT_DIR/templates/AGENTS.md" ./AGENTS.md
cp "$KIT_DIR/templates/一致性机制/文件联动目录.md" \
  ./一致性机制/文件联动目录.md
cp "$KIT_DIR/templates/一致性机制/决策档案.md" \
  ./一致性机制/决策档案.md
cp "$KIT_DIR/LICENSE" ./一致性机制/LICENSE.project-consistency-kit
printf '@AGENTS.md\n' > CLAUDE.md
```

这一步不会创建 README;项目按自己的对外需求决定是否编写。`一致性机制/VERSION` 会随机制件复制,记录项目当前安装的正式套件版本。`.agents/skills/catchup` 与 `wrapup` 会随仓库复制,供 Codex 和兼容 Agent Skills 的 Harness 直接发现。Claude Code 与 Codex 本地客户端的 Stop 配置也会复制,最终共同调用 `.agents/hooks/wrapup-reminder.mjs`;Windows Codex 中间经过 `.agents/hooks/wrapup-reminder.ps1` 薄适配器。目标项目的决策档案来自空白模板,不会继承套件自身历史。

### 2. 填 PROJECT

删除初始化提示,填写背景、工作流程、项目地图、当前阶段与近期决策。以后 catchup 以它为事实正本。

### 3. 填 AGENTS

删除初始化提示,补项目格式、命名、测试、质量与权限规则。同步纪律保留;决策落点固定为 PROJECT。

验证 Claude 导入适配器:

```bash
test -f AGENTS.md
test ! -L CLAUDE.md
test "$(tr -d '\r\n' < CLAUDE.md)" = "@AGENTS.md"
```

原生 Windows PowerShell:

```powershell
if (-not (Test-Path -LiteralPath .\AGENTS.md -PathType Leaf)) { throw 'AGENTS.md missing' }
if ((Get-Content -LiteralPath .\CLAUDE.md -Raw).Trim() -ne '@AGENTS.md') { throw 'invalid CLAUDE adapter' }
```

### 4. 配领域联动与二进制策略

- 在 `一致性机制/文件联动目录.md` Part A 登记 ADR、API 契约、大纲、风格规范等真实中枢。
- README 只有承载需要持续同步的公开安装 / API / 使用契约时才加入 Part A。
- 在项目自定规则区补稳定的“改 A 必查 B”。
- 纯代码 / 纯文字项目可删减 `.gitattributes` 和二进制忽略规则;需要 LFS 的项目保留并在 Git 初始化后执行 `git lfs install --local`。

### 5. 纳入 Git 并建立 horizon

```bash
git init
git branch -M main
git lfs install --local               # 仅 LFS 项目需要
git add -A
git commit -m "启用项目一致性机制:初始骨架"
git config --local projectConsistency.canonicalBranch main
node .agents/skills/wrapup/scripts/synced-guard.mjs advance
```

canonical branch 由用户确认并保存在当前 clone 的本地 Git config,不写入 PROJECT;新 clone 第一次使用时需重新确认。`synced` 只允许由 guard 在 canonical branch 创建或推进,feature branch 使用它与 canonical 的 merge-base 检查本分支变化。远端与 push 由项目负责人自行配置;多机使用需额外同步 `synced` tag。

## 开张后

- 新会话执行 catchup(Codex: `$catchup`;Claude Code: `/catchup`):harness 已注入 AGENTS / CLAUDE,工作流只读取 PROJECT、Git 与焦点文件。
- 收尾执行 wrapup(Codex: `$wrapup`;Claude Code: `/wrapup`):补决策、按 branch 基线检查联动并经确认后提交;canonical 通过 guard 推进 `synced`,feature 只保存 checkpoint。
- 首次 Claude Code 会话确认 Stop hook 权限;Codex 本地客户端还需信任项目 `.codex` 层并审查命令 hook,Codex CLI 可用 `/hooks` 查看状态。

## 升级

需要升级时,再次告诉 Agent“给这个项目引入一致性机制”,也可以通过当前 Harness 显式选择 `project-consistency-installer`。安装器会更新干净 Release 缓存,先比较 `一致性机制/VERSION` 的 SemVer,再比较修订日期与实际内容(同版或同日也不跳过 diff),保留 PROJECT / AGENTS 的项目内容、验证 CLAUDE 导入适配器,并逐条合并联动规则;不会创建或覆盖 README。v1.3.0+ 首次运行还会请用户确认 canonical branch,把它写入本地 Git config,并把初始 `synced` 建立动作委托给 guard。所有必需机制件验证成功后才最后推进 VERSION;部分升级不会伪装成完整版本。旧版若曾把套件决策泄漏进目标档案,只在精确命中已知记录且用户确认后删除对应行,其他归档内容不动。
