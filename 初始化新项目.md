# 在新项目里启用 Project Consistency Kit

> 新会话用 `/catchup` 读取项目事实与 Git 状态,收尾用 `/同步` 检查联动、提交并推进 `synced`。
> 机制设计见 [`一致性机制/机制设计说明.md`](一致性机制/机制设计说明.md)。

## 文件职责

```text
README.md                  项目自己的对外说明(可选,套件不创建)
PROJECT.md                 项目事实、地图、阶段与近期决策
AGENTS.md                  Agent 工作规则的唯一实体正本
CLAUDE.md -> AGENTS.md     Claude Code 相对链接适配
```

套件仓库与分发模板分开存放:

```text
project-consistency-kit/
├── README.md                         套件 GitHub 主页(不拷)
├── PROJECT.md                        套件自身事实(不拷)
├── AGENTS.md                         套件自身规则(不拷)
├── CLAUDE.md -> AGENTS.md            套件自身适配入口(不拷)
├── templates/
│   ├── PROJECT.md                    新项目事实骨架
│   ├── AGENTS.md                     新项目规则骨架
│   └── 一致性机制/文件联动目录.md      新项目联动规则骨架
├── .claude/
│   ├── settings.json                 Stop hook 接线
│   └── commands/
│       ├── catchup.md
│       ├── 同步.md
│       └── 引入一致性机制.md           机器级安装器(不拷)
├── 一致性机制/
│   ├── README.md
│   ├── 机制设计说明.md
│   ├── 文件联动目录.md                 套件自身真实规则(不拷)
│   ├── 决策档案.md
│   ├── hooks/收尾提醒.sh
│   └── skills/                        机器级安装器 skill(不拷)
├── CHANGELOG.md                       套件版本史(不拷)
├── LICENSE                            套件许可证(复制到机制子目录)
└── 初始化新项目.md                    本指南(不拷)
```

> 已有文件 / Git 历史的项目不要走下面的 rsync。请在目标项目运行全局 `/引入一致性机制`,由安装器拆分旧 CLAUDE / AGENTS 内容并建立链接。

## 套件首次落机(每台机器一次)

```bash
KIT_DIR="/你的绝对路径/project-consistency-kit"
mkdir -p ~/.claude/commands
ln -sfn "$KIT_DIR/.claude/commands/引入一致性机制.md" \
  ~/.claude/commands/引入一致性机制.md

mkdir -p ~/.agents/skills
ln -sfn "$KIT_DIR/一致性机制/skills/引入一致性机制" \
  ~/.agents/skills/引入一致性机制
```

两种入口指向同一安装器正本。套件迁移后需重建绝对链接。

## 绿地开张

### 1. 复制项目无关机制件

在全新项目根执行,`KIT_DIR` 指向套件仓库:

```bash
rsync -a \
  --exclude='.git' \
  --exclude='.DS_Store' \
  --exclude='README.md' \
  --exclude='PROJECT.md' \
  --exclude='AGENTS.md' \
  --exclude='CLAUDE.md' \
  --exclude='LICENSE' \
  --exclude='初始化新项目.md' \
  --exclude='CHANGELOG.md' \
  --exclude='docs/' \
  --exclude='templates/' \
  --exclude='.claude/commands/引入一致性机制.md' \
  --exclude='一致性机制/文件联动目录.md' \
  --exclude='一致性机制/skills/' \
  "$KIT_DIR/" ./

cp "$KIT_DIR/templates/PROJECT.md" ./PROJECT.md
cp "$KIT_DIR/templates/AGENTS.md" ./AGENTS.md
cp "$KIT_DIR/templates/一致性机制/文件联动目录.md" \
  ./一致性机制/文件联动目录.md
cp "$KIT_DIR/LICENSE" ./一致性机制/LICENSE.project-consistency-kit
ln -s AGENTS.md CLAUDE.md
```

这一步不会创建 README;项目按自己的对外需求决定是否编写。

### 2. 填 PROJECT

删除初始化提示,填写背景、工作流程、项目地图、当前阶段与近期决策。以后 `/catchup` 以它为事实正本。

### 3. 填 AGENTS

删除初始化提示,补项目格式、命名、测试、质量与权限规则。同步纪律保留;决策落点固定为 PROJECT。

验证适配链接:

```bash
test -f AGENTS.md
test -L CLAUDE.md
test "$(readlink CLAUDE.md)" = "AGENTS.md"
cmp AGENTS.md CLAUDE.md
```

### 4. 配领域联动与二进制策略

- 在 `一致性机制/文件联动目录.md` Part A 登记 ADR、API 契约、大纲、风格规范等真实中枢。
- README 只有承载需要持续同步的公开安装 / API / 使用契约时才加入 Part A。
- 在项目自定规则区补稳定的“改 A 必查 B”。
- 纯代码 / 纯文字项目可删减 `.gitattributes` 和二进制忽略规则;需要 LFS 的项目保留并在 Git 初始化后执行 `git lfs install --local`。

### 5. 纳入 Git 并建立 horizon

```bash
git init
git branch -M main
git lfs install --local               # 仅 LFS 项目需要
git add -A
git commit -m "启用项目一致性机制:初始骨架"
git tag synced
```

远端与 push 由项目负责人自行配置;多机使用需额外同步 `synced` tag。

## 开张后

- 新会话运行 `/catchup`:harness 已注入 AGENTS / CLAUDE,命令只读取 PROJECT、Git 与焦点文件。
- 收尾运行 `/同步`:补决策、检查联动、经确认后提交并推进 `synced`。
- 首次 Claude Code 会话确认 Stop hook 权限;Codex 直接读取 AGENTS。

## 升级

重跑 `/引入一致性机制`。安装器会比较版本、保留 PROJECT / AGENTS 的项目内容、验证 CLAUDE 链接,并逐条合并联动规则;不会创建或覆盖 README。
